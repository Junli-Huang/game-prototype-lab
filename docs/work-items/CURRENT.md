# Current Work Item

Active task:

```text
docs/work-items/EXP-003-equipment-vs-loot-space.md
```

Work / Developer:

1. Read the active task file completely.
2. Also read root `README.md`, `docs/workflow.md`, `docs/asset-handoff.md`, the EXP-001 / EXP-003 Backlog entries, `prototypes/001_spatial_backpack/README.md`, and `prototypes/001_spatial_backpack/assets/README.md`.
3. Implement only the scope in the active task.
4. Reuse Prototype #001 as the container; do not create a second backpack implementation or shared Inventory framework.
5. Use all specified `Assets Ready` SVG files directly; do not redraw them unless unusable and the reason is recorded.
6. Preserve EXP-001 gameplay and its historical MAYBE Player Result unchanged.
7. EXP-003 Light / Heavy conditions must use the same fixed Loot sequence; only starting locked Equipment occupancy differs.
8. Build, deploy / verify Pages, synchronize required documentation/status, then stop at EXP-003 TESTING / Result: Untested. Do not pre-fill Player conclusions.
