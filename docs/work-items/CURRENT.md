# Current Work Item

Active task:

```text
docs/work-items/EXP-007-008-world-refresh-profile-r2.md
```

Work / Developer:

1. Read the active task file completely.
2. Also read root `README.md`, `docs/workflow.md`, `docs/asset-handoff.md`, the EXP-007 / EXP-008 Backlog entries, `prototypes/002_campfire_respawn/README.md`, and `prototypes/002_campfire_respawn/assets/README.md`.
3. Implement only the scope in the active task.
4. Use all specified `Assets Ready` files directly; do not remake them unless they cannot be used and the reason is recorded.
5. Preserve historical EXP-007 / EXP-008 implementation notes and Player results. This task creates the R2 comparison condition; do not rewrite R1 history.
6. Keep EXP-007 and EXP-008 under the same selected Refresh Profile; only their World Refresh trigger may differ.
7. Build, deploy / verify Pages, synchronize required documentation, then stop with R2 gameplay Result still Untested. Do not pre-fill Player conclusions.
